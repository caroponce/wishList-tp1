import { StyleSheet } from "react-native";
import { COLORS } from "../../themes";

export const styles = StyleSheet.create({
    container: {
       
        marginHorizontal: 5,
      },
      categoryList:{
        gap: 15,
        
      },
      categoryContainer: {
        marginHorizontal: 10,
        marginTop: 10,
      }
  });